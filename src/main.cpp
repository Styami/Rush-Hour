#include "Sommets.hpp"
#include "Graph.hpp"
#include "console_app.hpp"

int main(){
    srand(time(NULL));
    
    // TODO: faire la création du graph
    Window window(16);

    window.main_loop();

    return 0;
}